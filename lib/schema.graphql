type Course {
  _id: ID!,
  title: String!
  teacher: String
  description: String!
  topic: String
}
type Query {
  "Return all the courses"
  getCourses: [Course]

  "Return a course"
  getCourse(id: ID!): Course
}